#include <bits/stdc++.h>
using namespace std;
const int N = 2e5+5;
int hsh[N];
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin>>t;
    while (t--){
        int n; cin>>n;
        vector<int> v(n,0);
        for (auto &x: v) {cin>>x; hsh[x]++;}
        int k=n;
        while (k--){
            for (int i=)

    }
}